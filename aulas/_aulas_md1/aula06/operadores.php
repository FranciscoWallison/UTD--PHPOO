<?php
	$a = 2;
	$b = 3;

	// Soma 
	$some = $a + $b;

	//Subtração
	$subtracao = $a - $b;

	//Multiplicar
	$multiplicacao = $a*$b;

	//Divisão
	$divisao = $a/$b;

	//Incremento
	#Adicionar +1 na minha variável
	$a++;

	#Adicionar +2 na minha variável
	$a+2;

	//Decrementar
	#Subtrair -1 na minha variável
	$a--;

	#Subtrair -2 na minha variável
	$a-2;

	//Operadores Matématicos
	# > 	(maior que)
	# < 	(menor que)
	# <= 	(menor ou iqual)
	# >=	(meno ou iqual)
	# %		(Resto da divisão)
	# ^		(elevado à ['expoente'])