-- Códigos de uso do SQL -- 

-- Comando básico para abrir o mysql no terminal--

	#mysql -u root -p (insira a senha)

-- Criando um banco de dados --
	#Observação: as nomeclaturas de banco de dados e de tableas devem ser entre CRASE (``). Além da sintaxe dos comandos serem  escritos em MAIUSCULO
CREATE DATABASE `nome_do_banco`;

-- Criando uma tabela de bancodados --
CREATE TABLE `nome_da_tabela`(`nome_do_campo1` TIPO(tamanho),(`nome_do_campo2` TIPO(tamanho) ,(`nome_do_campoN` TIPO(tamanho));
